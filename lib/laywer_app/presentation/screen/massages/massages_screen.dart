import 'package:flutter/cupertino.dart';

class MassagesScreen extends StatelessWidget {
  const MassagesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("MassagesScreen"),
    );
  }
}
